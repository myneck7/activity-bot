//those commands have to be run at least once in the bot folder to create the node_modules folder and download the dependencies

node -v //check your version of node, should be higher than V16.6.0

npm install discord.js
npm install @discordjs/builders @discordjs/rest discord-api-types
npm install discord-akairo

//before pushing anything, check that the config.json and the node_modules files are in the .gitignore
